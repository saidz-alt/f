<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      KID-SAFETY / COPPA POSTURE
      ==========================
      This release manifest declares NO permissions on purpose:
        * No INTERNET           -> the app literally cannot send data anywhere.
        * No storage/media perms-> progress lives in app-private storage only.
        * No microphone/camera  -> nothing is recorded.
      The INTERNET permission exists ONLY in the debug manifest (for Flutter
      hot-reload during development) and is never present in a release build.
      All learning content, audio SFX, and pronunciation are bundled or use
      the on-device TTS engine, so the app is fully functional offline.
    -->

    <application
        android:label="KabyleDuo"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:usesCleartextTraffic="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <!-- Lets flutter_tts discover on-device Text-To-Speech engines on
             Android 11+ (package visibility). -->
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE"/>
        </intent>
        <!-- Used by the Flutter engine's ProcessText plugin. -->
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
